// types/enums.ts

export enum UserRole {
    Faculty = 'faculty',
    Admin = 'admin',
  }
  
  export enum HallBlock {
    LHC = 'LHC',
    ESB = 'ESB',
    DES = 'DES',
    APEX = 'APEX',
  }
  
  export enum BookingStatus {
    Pending = 'pending',
    Approved = 'approved',
    Rejected = 'rejected',
  }
  